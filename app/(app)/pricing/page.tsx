import PricingClient from './client';

export const metadata = {
    title: 'Pricing — VOXLY',
    description: 'Simple, transparent pricing for AI voiceovers. Start free, scale as you grow.',
};

export default function PricingPage() {
    return <PricingClient />;
}
